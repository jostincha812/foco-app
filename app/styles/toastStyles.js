import T from '../T'
import text from './textStyles'

export default toastStyles = {
  toast: {
    textStyle: text.footnote,
    position: -60,
  },
  success: {
    // backgroundColor: T.colors.success,
  },
  error: {
    backgroundColor: T.colors.error,
  }
}
